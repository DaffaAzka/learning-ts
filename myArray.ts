const superHeroes: string[] = []
const heroesPower: Array<number> = []

type User = {
    name: string,
    isActive: boolean
}

const allUser: Array<User> = []
 
superHeroes.push('Batman');

allUser.push({name: "Dest", isActive: true});

const MLMOdels: number[][] = [
    [255, 255, 255],
]